#!/usr/bin/python2.7
import requests
from hosted import device, node, config
config.restart_on_update()

device.gpio.monitor(config.pin)
for (pin, state) in device.gpio.poll_forever():
    while state == True:
        r = requests.get('https://ap.telegram.org/bot545310390:AAHWO6DJCvimklYkFJKxYRbhwJwnE2HLubg/sendMessage?chat_id=-1001336256280&text=hey3')
